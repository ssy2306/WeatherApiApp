<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Weather App</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <!-- Adjust the form action to match your server route -->
        <form action="/getWeather" method="post">
          <!-- Make sure the input field name matches your server's expected field name (e.g., "cities") -->
          <input name="cities" type="text" class="ghost-input" placeholder="Enter cities (comma-separated)" required>

          <input type="submit" class="ghost-button" value="Get Weather">
        </form>

        <!-- These sections are for displaying weather information and error messages returned from your server -->
        <% if(weather !== null) { %>
          <h3>Weather Information</h3>
          <ul>
            <% for (var city in weather) { %>
              <li><strong><%= city %>:</strong> <%= weather[city] %></li>
            <% } %>
          </ul>
        <% } %>

        <!-- Display error messages if there are any -->
        <% if(error !== null) { %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
